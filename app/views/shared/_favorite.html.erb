      <% if restaurant.is_favorited?(current_user) %>
        <%= link_to '',unfavorite_restaurant_path(restaurant), method: :post, class: "glyphicon glyphicon-heart"  %>
      <% else %>
        <%= link_to '',favorite_restaurant_path(restaurant), method: :post, class: "glyphicon glyphicon-heart-empty" %> 
      <% end %>